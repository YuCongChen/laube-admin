version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - /Users/laube/workspace/database-data/laube/redis:/data
    command: redis-server --appendonly yes --requirepass 123456